// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyC82QX1NKLehff64WcP6mg6iMCUmhhwKMY",
    authDomain: "friendly-chat-8c06f.firebaseapp.com",
    databaseURL: "https://friendly-chat-8c06f.firebaseio.com",
    projectId: "friendly-chat-8c06f",
    storageBucket: "friendly-chat-8c06f.appspot.com",
    messagingSenderId: "138931905550"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
